<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_santhe_connect"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_santhe_connect"
        android:supportsRtl="true"
        android:theme="@style/Theme.SantheConnect">

        <activity
            android:name="com.santheconnect.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SantheConnect">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Add your Google Maps API Key in local.properties -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="Enter-Your-Maps-API-Here" />

    </application>

</manifest>
